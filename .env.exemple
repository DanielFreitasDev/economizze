# Copie este arquivo para .env antes de executar no Docker.
# Exemplo:
# cp .env.exemple .env

# Aplicacao Spring Boot
SERVER_PORT=8080
DB_URL=jdbc:postgresql://host.docker.internal:5432/economizze
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Runtime do container
JAVA_OPTS=
TZ=America/Fortaleza
